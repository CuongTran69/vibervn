# Tasks: terminal-animations

## 1. Setup

- [x] 1.1 Install `react-type-animation` package

## 2. CSS Animations

- [x] 2.1 Add cursor blink keyframe to `globals.css`
- [x] 2.2 Add terminal fade-in staggered animation to `globals.css`

## 3. Client Component

- [x] 3.1 Create `src/components/terminal-typing.tsx` — client component with TypeAnimation cycling tool names

## 4. Hero Section Updates

- [x] 4.1 Add cursor blink `▌` to `[ GATEWAY ]` badge
- [x] 4.2 Replace static hero title with terminal typing component (`$ connect --with {typing}`)

## 5. Quick Start Terminal Block

- [x] 5.1 Restyle Quick Start as terminal block (dark bg, green text, dot header, `$` prompts, `✓` results)

## 6. Verification

- [x] 6.1 Run `npm run build` — confirm successful build (+3.1KB bundle)

